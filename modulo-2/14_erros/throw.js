const x = 10

// Checar se x é um número
if (!Number.isInteger(x)) { // Converte para número antes de checar
    console.log("x não é um número");
} else {
    console.log("x é um número");
}

console.log("Continuando o código");
